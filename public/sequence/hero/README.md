# Hero frame sequence

Drop the exported frames here, named:

    hero_0001.webp
    hero_0002.webp
    ...

Then set `frameCount`, `width`, `height` and `ext` in `lib/sequence.ts`
to match the delivered render.
